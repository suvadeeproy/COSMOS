# COSMOS — Scientific ML Workbench (Final)

A hiring-grade, free, and reproducible scientific-ML dashboard.

**Modules**
1. Exoplanets (real data via `lightkurve` + BLS; synthetic fallback)
2. Landscape surrogates (GBM + SHAP + conformal intervals)
3. Soft-wall AdS/QCD (finite-difference SL solver + convergence)
4. Benchmarks (local CSV demos; no network)
5. About (citations, licensing)

## Local run
```bash
pip install -r requirements.txt
streamlit run app.py
```
