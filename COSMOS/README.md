# COSMOS — Physics‑Plus Build (v5)

This release adds **plot‑by‑plot physics explanations** on every page and a tighter **About** page so users learn
what each graphic says about the system and the underlying principles.

## Run
```bash
pip install -r requirements.txt
streamlit run app.py
```
