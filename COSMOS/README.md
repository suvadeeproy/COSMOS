# COSMOS — Physics‑Plus Build (v6)

This build keeps the functionality intact and revises all user‑visible prose to remove meta‑commentary,
replace parenthetical asides with **concise, physics‑first statements**, and add **explicit formulas** where useful.

## Run
```bash
pip install -r requirements.txt
streamlit run app.py
```
