# COSMOS — Physics‑Plus Build (v6, Math‑Enriched)

This edition adds **compact derivations and formal statements** in every module:
- Exoplanets: BLS objective, transit geometry, SNR and aliasing math.
- Landscape: split‑conformal prediction guarantee, permutation importance, partial dependence.
- AdS/QCD: Sturm–Liouville operator, boundary conditions, WKB quantization, virial check, discretization error order.

Run:
```bash
pip install -r requirements.txt
streamlit run app.py
```
